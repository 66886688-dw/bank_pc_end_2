<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bank.dao.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.bank.entity.SysUser">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="id_card" property="idCard"/>
        <result column="phone" property="phone"/>
        <result column="trade_password" property="tradePassword"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, id_card, phone, trade_password, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.bank.entity.SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (name, id_card, phone, trade_password)
        VALUES (#{name}, #{idCard}, #{phone}, #{tradePassword})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE id = #{id}
    </select>

    <select id="selectByIdCard" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE id_card = #{idCard}
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE phone = #{phone}
    </select>

</mapper>
